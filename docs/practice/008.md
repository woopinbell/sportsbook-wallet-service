> 생성물. 직접 수정 금지. 답: ../commits/008.md

# WalletService 통합 테스트 (Testcontainers)

실제 PostgreSQL + Redis 컨테이너 위에서 happy path / 거부 / 멱등성을 덮고 100건 동시 debit race를 증명한다.

## 개요

Testcontainers는 테스트 실행 중 실제 Docker 컨테이너(`postgres:16-alpine`, `redis:7-alpine`)를 띄우는 라이브러리다. mock이나 in-memory H2 대신 진짜 PostgreSQL을 쓰므로, `SELECT FOR UPDATE`와 unique 제약 같은 DB 고유 동작이 테스트에서도 그대로 검증된다.

여덟 테스트가 세 nested 그룹으로 나뉜다. Kafka auto-configuration은 이 슬라이스에서 제외한다. outbox 테스트(012)가 Kafka 컨테이너를 따로 가져올 것이고, 여기서 띄우면 새 경로 검증 없이 시작 시간만 10초 넘게 늘기 때문이다.

## 작업 순서

1. `@Testcontainers`로 PostgreSQL + Redis 컨테이너를 띄운다.
2. `@DynamicPropertySource`로 컨테이너 접속 정보를 Spring 프로퍼티에 주입한다.
3. happy path 3 / 거부 2 / 멱등성 3 테스트를 짠다.
4. 100건 동시 debit을 `CompletableFuture`로 쏴 race를 증명한다.

## 작업 내역

### 컨테이너 배선

`@Container` 선언은 계약이라 노출한다. 프로퍼티 주입 본문은 비운다.

```java
@Container static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine");
@Container static GenericContainer<?> redis = new GenericContainer<>("redis:7-alpine").withExposedPorts(6379);

@DynamicPropertySource
static void registerProperties(DynamicPropertyRegistry registry) {
  // TODO: postgres jdbcUrl/username/password, redis host/port를 spring.datasource.* / spring.data.redis.* 로 주입
}
```

`application-test.yml`은 datasource URL을 비워둔다. 각 통합 테스트가 자기 `@DynamicPropertySource`로 컨테이너를 주입할 것을 강제하고, 빠뜨리면 오래된 로컬 PostgreSQL에 조용히 붙는 대신 `NoSuchBeanDefinitionException`으로 드러나게 한다.

### 세 그룹

**책임:** 세 nested 그룹이 각 경로를 덮는다. happy/거부/멱등성. 케이스 의도만 적고 setup·assertion은 채운다.

- **HappyPath(3)**: deposit→debit→credit이 계좌를 원래 available로 되돌리고 locked를 0으로 만든다. 전체 테이블의 차변−대변이 0으로 수렴한다. withdraw가 EXTERNAL_PAYMENT 상대로 쌍을 쓴다. HOUSE_POOL credit이 수익을 지급하되 합 = 0이 유지된다.
- **Rejection(2)**: available 초과 debit이 `InsufficientBalanceException`을 던지고 ledger에는 원래 deposit 쌍만 남는다. KRW 계좌에 USD deposit이 `CurrencyMismatchException`.
- **Idempotent(3)**: 같은 키 두 번 호출이 두 번째에 같은 `operation_group_id`를 돌려주고 쌍을 더 만들지 않는다. 같은 키 다른 금액이 `IdempotencyConflictException`.

### 100건 동시 debit

**책임:** 같은 키로 100개 요청을 동시에 쏴, 정확히 하나의 분개 쌍만 생기고 모든 결과의 `operation_group_id`가 하나로 수렴하며 합 = 0이 유지됨을 본다. race-loser의 unique 위반 warning은 `WalletService` 재시도 handler가 흡수한다(테스트 실패 아님). 이것이 부하 테스트(019)가 더 큰 규모로 다시 돌릴 Level 3 정합성 증명이다.

```java
int parallelism = 100;
ExecutorService exec = Executors.newFixedThreadPool(20);
CountDownLatch start = new CountDownLatch(1);
// TODO: 100개 future가 start.await()로 대기 → start.countDown()으로 동시에 같은 키 debit
// TODO: 검증 — 결과 operation_group_id 단일 수렴, ledger pair 정확히 1개, 합 = 0
```

## 검증

- `mvn -q -Dtest=WalletServiceIntegrationTest test` — 여덟 테스트가 모두 green. *Testcontainers(PostgreSQL+Redis, Docker)가 떠야 한다.*
- 감각 점검: 100건 동시 debit 뒤 `ledger_entry`에 그 키의 쌍이 정확히 *하나*인가(락/제약이 직렬화했다는 증거) / race-loser warning이 로그엔 보여도 테스트 실패로는 안 올라오는가.
